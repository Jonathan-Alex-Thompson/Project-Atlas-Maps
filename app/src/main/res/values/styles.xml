<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/atlasEcho</item>
        <item name="colorPrimaryDark">@color/atlasDark</item>
        <item name="colorAccent">@color/atlasApollo</item>
        <item name="android:windowBackground">@color/atlasClean</item>
    </style>

</resources>
